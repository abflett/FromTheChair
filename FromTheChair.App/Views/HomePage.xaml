<Page x:Class="FromTheChair.App.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="36" Spacing="24" MaxWidth="820" HorizontalAlignment="Stretch">
            <StackPanel Spacing="10">
                <TextBlock Text="YOUR EVERYDAY MOVEMENT" FontSize="12" FontWeight="SemiBold"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock Text="Make room for a little movement." FontSize="34" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBlock Text="Simple routines. Small breaks. At your own pace." TextWrapping="Wrap"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="28">
                <StackPanel Spacing="18">
                    <FontIcon Glyph="&#xE916;" FontSize="28" HorizontalAlignment="Left"
                              Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <TextBlock Text="Start with a rhythm that fits." Style="{ThemeResource SubtitleTextBlockStyle}" TextWrapping="Wrap" />
                    <TextBlock Text="Choose how often you'd like to take a movement break. You can change it whenever your day changes."
                               TextWrapping="Wrap" MaxWidth="570" HorizontalAlignment="Left"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <Button Content="Set your preferences" Style="{ThemeResource AccentButtonStyle}" Click="Configure_Click" />
                </StackPanel>
            </Border>
            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                <StackPanel Spacing="8">
                    <TextBlock Text="Preferred break interval" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="{x:Bind ViewModel.SavedCadence, Mode=OneWay}" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="8">
                    <TextBlock Text="Reminders" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="Not active yet" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
            <InfoBar IsOpen="True" IsClosable="False" Severity="Informational" Title="A place to begin"
                     Message="You can save your preferences today. Exercise sessions and reminders are coming next." />
        </StackPanel>
    </ScrollViewer>
</Page>
